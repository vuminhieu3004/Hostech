Các câu lệnh được setup:

#chạy cả BE và FE :
npm start

# Cài đặt dependencies cho cả FE và BE
npm run install:all

# Chỉ chạy backend
npm run backend

# Chỉ chạy frontend  
npm run frontend

# Build frontend (production)
npm run build

# Chạy migration
npm run migrate

# Reset database và seed lại
npm run migrate:fresh

# Test backend
npm run test:backend

# Lint frontend
npm run lint:frontend




// env 


APP_NAME=Hostech
APP_ENV=local
APP_KEY=base64:G6TcF+l+X6gpGMTkJWz/mw3q8Qct0KIasv/foyedSPA=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000
FRONTEND_URL=http://localhost:3000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=datn_hostech
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=vutienhieu202@gmail.com
MAIL_PASSWORD=cxvtglghyabzkqqj
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="vutienhieu202@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

JWT_SECRET=base64:G6TcF+l+X6gpGMTkJWz/mw3q8Qct0KIasv/foyedSPA=
JWT_TTL_MINUTES=60
OTP_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=5
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCK_MINUTES=15
REFRESH_TTL_DAYS=30

ESMS_API_KEY=644944B1253D631B0F6F0D982189D4
ESMS_SECRET_KEY=3F94FF72758EEAF8EDF10D6CBA546C
ESMS_BRAND_NAME=Hostech
